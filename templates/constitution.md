# [PROJECT_NAME] 项目宪法

> 这是项目的最高规范。所有后续开发必须遵守以下原则，修改需经显式讨论。

## 不可协商原则

### 质量底线
- 测试覆盖率: service 层 ≥ 85%，API 层 ≥ 70%
- 所有测试必须通过才能提交（`--force` 除外）
- 禁止裸 `except` / `catch`，必须指定异常类型

### 安全红线
- 禁止在代码中硬编码 secret/token/key
- 所有用户输入必须经过显式验证
- 数据库操作必须使用参数化查询

### 架构边界
- 禁止绕过 [架构模式，如：service 层不许写 SQL]
- 核心文件修改需先问用户

### 代码风格
- 所有函数必须有类型注解
- 禁止魔法数字（必须定义命名常量）
- 禁止生成超过 150 行的函数

## 测试要求
- 每个新功能必须有对应测试
- Bug fix 必须有回归测试

## 项目特定约束
[这里填写项目专属的、不可协商的约束]

## 变更记录
| 日期 | 版本 | 变更说明 |
|------|------|----------|
| YYYY-MM-DD | v1.0 | 初始版本 |